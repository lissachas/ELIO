# Базовые спецификации

**Цель языка:** быть максимально предсказуемым и безопасным.
Спецификация находится в процессе разработки.

### Основные принципы:

1. **Закон безопасности (Safety law)**
2. **Закон детерминизма (Determinism law)** 
3. **Закон простоты (Simplicity law)** 

Безопасность >= Детерминизм >> Простота

---

# Типы данных

### Числовые типы:

* `int8`, `int16`, `int32`, `int64` - целые числа
* `uint8`, `uint16`, `uint32`, `uint64` - целые числа без знака
* `flo32`, `flo64` - одинарная точность

### Нечисловые типы:

* `char` - символ
* `string` - неизменяемая строка
* `buf_string` - изменяемая строка
* `string_view` - 
* `unit` - отсутствие значения (аналог `void`)
* `bool` - логический тип
* `optional[T]` - значение может отсутствовать
* `result[T, E]` - результат с возможной ошибкой
* `shared[T]` - 
* `weak[T]` - 

---

# Управляющие конструкции (циклы и ветвления)

* `if (...) else if (...) else (...)`
* `while (...)`
* `for (...)`
* `loop (...)`

* return - может возвращать или не возвращать значение
* break - 
* continue - 


---

# Выражения

* **Комментарии:** `// до конца строки`

* **Лямбда-функции:**

  * Полная форма: `() -> { return; }`
  * Короткая форма: `() -> ;`
  * Можно использовать в любом месте
  * Захват переменных — **только неизменяемые, по значению**

* **if как выражение** (возвращает значение)

* Match


---

# Объявления

* `:` используется для любых объявлений
* let
* const

---

### Контейнеры:

* Структуры (`Struct`)
* Массивы (`Arrays`)

---

### Функции:

---

### Паттерны:


---

### Арифметика

*   sign" "(" <expr> ")" -
*   "unsign" "(" <expr> ") -
*   "trunc_cast" "[" <type> "]" "(" <expr> ")" - 
*   "check_cast" "[" <type> "]" "(" <expr> ")" -
*   "wrap_add" "(" <expr> "," <expr> ")" -
*   "wrap_sub" "(" <expr> "," <expr> ")" -
*   "wrap_mul   "(" <expr> "," <expr> ")" -


### Модель памяти

* Reference counting - 
* Shared state


### Таргет

* Реализовано на LLVM
