module;

#include <string>
#include <unordered_map>
#include <string_view>
#include <vector>
#include <variant>
#include <concepts>
#include <stdexcept>

export module typecheck;

import lexer;
import tokens;
import error;
import parser;
import symbol;
import resolve;
import expr;


export class TypeChecker {
    public:

    private:

};